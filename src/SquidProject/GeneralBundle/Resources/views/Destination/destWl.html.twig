{% extends '::layoutSquid.html.twig' %}

{% block slider%}
	{% include '::slider.html.twig' %}

{%endblock %}

{% block menu%}
	{% include 'SquidProjectGeneralBundle:Menus:menuDestination.html.twig' %}

{%endblock %}

{% block corps %}
	<div style="padding-left:150px; padding-top:50px  " class="col-lg-8" >
		<div class="table-responsive">
            <table class="table table-bordered table-hover table-striped">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Base de donnée</th>
                        <th>Etat</th>
                        <th>Modifier</th>
                    </tr>
                </thead>
                <tbody>
                    {%for w in  wl%}
                    	<tr>
                    		<td>{{w.id}}</td>
                    		<td><a href="{{path('squid_project_general_destination_DB_content',{'id':w.id})}}">{{w.nom}}</a></td>
                    		<td> autorisé </td>
                    		<td><a href="{{path('squid_project_general_destination_DB_update',{'id':w.id})}}">
                                <button   style="float:right" class="btn btn-primary" id="up">
                                Bloquer
                            </button></a></td>
                    	</tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
	</div>

	
{% endblock %}