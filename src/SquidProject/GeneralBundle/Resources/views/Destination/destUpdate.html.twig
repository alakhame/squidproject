{% extends '::layoutSquid.html.twig' %}

{% block slider%}
	{% include '::slider.html.twig' %}

{%endblock %}

{% block menu %}
	{% include 'SquidProjectGeneralBundle:Menus:menuDestination.html.twig' %}

{%endblock %}

{% block corps %}
	<div style="padding-left:150px; padding-top:50px  " class="col-lg-8" >
		<form role="form" method="post" action="{{path("squid_project_general_destination_update",{'id':d.id })}}">
		<fieldset>

            <div class="form-group ">
                <label class="control-label" for="nom">Nom de la destination</label>
                <input type="text" class="form-control" id="nom" name="nom" value="{{d.nom}}">
            </div>
           
            <div class="form-group ">
                <label class="control-label" for="type">Base de données</label>
                <select class="form-control" name="idDB" id="idDB">
                	<option value=""></option>
                    {% for db in dbs %}
                        <option value="{{db.id}}">{{db.nom}}</option>
                    {% endfor %}
                </select>
            </div>
            <input type="hidden" class="form-control" id="id" name="id" value="{{d.id}}">

            <button type="submit" class="btn btn-primary">Modifier</button>
        </fieldset>
        </form>	
	</div>

{% endblock %}