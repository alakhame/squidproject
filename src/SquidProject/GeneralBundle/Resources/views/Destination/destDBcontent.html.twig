{% extends '::layoutSquid.html.twig' %}

{% block slider%}
	{% include '::slider.html.twig' %}

{%endblock %}

{% block menu%}
	{% include 'SquidProjectGeneralBundle:Menus:menuDestination.html.twig' %}

{%endblock %}

{% block corps %}
	<div class="col-lg-8" style="padding-left:100px;padding-top:20px" >
				<div class="row">	
					<div class="col-lg-9"> <h4> Contenu de la base de donnée : <span style="color:blue"> {{db.nom}} </span> </h4> </div>
					<div class="col-lg-3"> 
						<a href="{{path('squid_project_general_domaine_add',{'idDB':db.id})}}"> 
							<button    class="btn btn-primary" id="add">
                               Ajouter Domaines
                            </button>
                        </a> 
                    </div>	
                </div>

				{%for dom in doms%}
					<div class="row"> 
						<div class="col-lg-8">{{dom.nom}}</div>
						<div class="col-lg-4">
							<a href="{{path('squid_project_general_domaine_delete',{'idDB':db.id,'id':dom.id})}}"> 
								<button    class="btn btn-primary" id="del">
                                Supprimer
                            	</button>
                            </a>
                        </div>
                    </div>
						
					{%endfor%}
	</div>
{% endblock %}